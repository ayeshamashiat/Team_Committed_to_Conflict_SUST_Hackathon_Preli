# QueueStorm Investigator environment variables
# Copy this file to .env and fill in real values locally. Do NOT commit .env.

# Groq API key (optional — pipeline is rule-based and runs without it)
GROQ_API_KEY=

# Model name (only consulted if Groq is wired in later)
MODEL_NAME=llama-3.3-70b-versatile

# Server port
PORT=8000

# Timeouts
REQUEST_TIMEOUT_SECONDS=30
HEALTH_TIMEOUT_SECONDS=60